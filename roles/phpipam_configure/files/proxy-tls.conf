# BunkerWeb terminates TLS upstream; phpIPAM reads $_SERVER['HTTPS'] to
# build absolute URLs. Do NOT set ServerName here: it lands in the *:80
# vhost and breaks direct-IP access.
SetEnvIf X-Forwarded-Proto "https" HTTPS=on
